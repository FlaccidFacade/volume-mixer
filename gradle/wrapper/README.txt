The gradle-wrapper.jar file needs to be generated when internet access is available.

To generate it, run:
    gradle wrapper --gradle-version 8.4

This will download the necessary wrapper JAR file from Gradle's distribution server.

Alternatively, if you have Android Studio installed, you can:
1. Open the project in Android Studio
2. Android Studio will automatically download and configure the Gradle wrapper
